package com.rhseung.glance.tooltip.builder

import com.rhseung.glance.draw.Padding
import com.rhseung.glance.icon.AttributeIcon
import com.rhseung.glance.icon.SlotIcon
import com.rhseung.glance.util.Draw

class TooltipBuilder {
    init {
//        val original = AttributeIcon.ATTACK_DAMAGE + 1.0 + AttributeIcon.ATTACK_SPEED + 1.6;
//
//        var new = original;
//        if (showSlot)
//            new = original.add(SlotIcon.MAINHAND).add(">", Padding.SLOT).add(original);
//            new = SlotIcon.HAND + ">" + Padding.SLOT + original;
//
//        builder
//            .add(AttributeIcon.ATTACK_DAMAGE, 1.0)
    }
}